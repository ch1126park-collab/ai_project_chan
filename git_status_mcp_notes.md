# 현재 프로젝트 Git 상태 및 MCP 메모

- 현재 Git 상태
  - 브랜치: `master`
  - 작업 트리 상태: 변경 사항 없음
  - 확인 명령: `git status --short --branch`

- MCP란 무엇인가
  - MCP는 `Model Context Protocol`의 약자입니다.
  - 외부 도구, 파일, 데이터베이스, 서비스의 정보를 모델이 일정한 방식으로 읽도록 연결하는 규약입니다.
  - MCP가 연결되어 있으면 모델은 MCP 서버가 제공하는 리소스나 템플릿을 통해 데이터를 읽을 수 있습니다.

- 현재 Git 상태를 읽을 때 사용하는 방식
  - 사용 방식: MCP가 아니라 로컬 셸 명령 사용
  - 사용 명령: `git status --short --branch`
  - 이유: 현재 세션에서 연결된 MCP 리소스와 템플릿이 없음

- 현재 확인된 MCP 상태
  - MCP 리소스 목록: 없음
  - MCP 리소스 템플릿 목록: 없음
  - 의미: Git 상태를 읽기 위한 전용 MCP 서버가 현재 설정되어 있지 않음

- 정리
  - 이 프로젝트의 현재 Git 상태 확인은 MCP 기반이 아님
  - 현재는 터미널의 `git` 명령으로 상태를 읽고 있음
